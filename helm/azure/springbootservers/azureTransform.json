{%- assign resource = content.properties -%}
{%- assign metadata = context -%}

{
  "metadata: {
    "name": {{ metadata.AzureContext.azureUID | json }},
    "namespace": "springbootsites"
  }
  "spec": {
    "server": {{ resource.server }}
  }
}